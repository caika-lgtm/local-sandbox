name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always
  LSB_FORCE_DOCKER_ROOTFS: "1"

jobs:
  build-os-image:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-aarch64
            runner: ubuntu-24.04-arm
            guest_linker_env: CARGO_TARGET_AARCH64_UNKNOWN_LINUX_MUSL_LINKER
            guest_linker_bin: musl-gcc
          - platform: macos-x86_64
            runner: ubuntu-24.04
            guest_linker_env: CARGO_TARGET_X86_64_UNKNOWN_LINUX_MUSL_LINKER
            guest_linker_bin: musl-gcc
          - platform: windows-x86_64
            runner: ubuntu-24.04
            guest_linker_env: CARGO_TARGET_X86_64_UNKNOWN_LINUX_MUSL_LINKER
            guest_linker_bin: musl-gcc
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Resolve platform metadata
        run: cargo run -p xtask -- platform-meta --platform "${{ matrix.platform }}" --format env --version "${GITHUB_REF_NAME}" >> "$GITHUB_ENV"

      - name: Add guest target
        run: rustup target add "${LSB_GUEST_TARGET}"

      - name: Install musl toolchain and kernel build deps
        run: |
          sudo apt-get update
          sudo apt-get install -y musl-tools build-essential bc flex bison \
            libelf-dev libssl-dev wget xz-utils

      - name: Build lsb-guest
        run: env "${{ matrix.guest_linker_env }}=${{ matrix.guest_linker_bin }}" cargo build -p lsb-guest --target "${LSB_GUEST_TARGET}" --release

      - name: Build OS image
        run: cargo run -p xtask -- prepare-rootfs --platform "${{ matrix.platform }}"

      - name: Package OS image
        run: cargo run -p xtask -- package-release --platform "${{ matrix.platform }}" --artifact os-image --version "${GITHUB_REF_NAME}"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: os-image-${{ matrix.platform }}
          path: lsb-os-*.tar.gz
          retention-days: 1

  build-cli:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-aarch64
            runner: macos-14
            rust_target: aarch64-apple-darwin
          - platform: macos-x86_64
            runner: macos-14
            rust_target: x86_64-apple-darwin
          - platform: windows-x86_64
            runner: windows-latest
            rust_target: x86_64-pc-windows-msvc
    runs-on: ${{ matrix.runner }}
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust_target }}

      - name: Resolve platform metadata
        run: cargo run -p xtask -- platform-meta --platform "${{ matrix.platform }}" --format env --version "${GITHUB_REF_NAME}" >> "$GITHUB_ENV"

      - name: Build CLI
        run: cargo build -p lsb-cli --release --target "${LSB_HOST_TARGET}"

      - name: Codesign
        if: ${{ startsWith(matrix.platform, 'macos-') }}
        run: codesign --entitlements "${LSB_CODESIGN_ENTITLEMENTS}" --force -s - "target/${LSB_HOST_TARGET}/release/${LSB_CLI_BINARY}"

      - name: Stage CLI artifact
        run: |
          mkdir -p target/release
          cp "target/${LSB_HOST_TARGET}/release/${LSB_CLI_BINARY}" "target/release/${LSB_CLI_BINARY}"

      - name: Package CLI binary
        run: cargo run -p xtask -- package-release --platform "${{ matrix.platform }}" --artifact cli --version "${GITHUB_REF_NAME}"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: cli-binary-${{ matrix.platform }}
          path: lsb-*.tar.gz
          retention-days: 1

  validate-managed-qemu:
    name: Validate managed Windows QEMU artifact
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Resolve managed QEMU metadata
        run: cargo run -p xtask -- platform-meta --platform windows-x86_64 --format env >> "$GITHUB_ENV"

      - name: Verify managed QEMU artifact
        run: |
          echo "Managed QEMU package: ${LSB_MANAGED_QEMU_PACKAGE_VERSION}"
          echo "Managed QEMU URL: ${LSB_MANAGED_QEMU_URL}"
          curl -fsSL "${LSB_MANAGED_QEMU_URL}" -o "${LSB_MANAGED_QEMU_TARBALL}"
          echo "${LSB_MANAGED_QEMU_SHA256}  ${LSB_MANAGED_QEMU_TARBALL}" | sha256sum -c -

  release:
    needs: [build-os-image, build-cli, validate-managed-qemu]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: os-image-*
          merge-multiple: true
          path: artifacts/

      - uses: actions/download-artifact@v4
        with:
          pattern: cli-binary-*
          merge-multiple: true
          path: artifacts/

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: artifacts/*
